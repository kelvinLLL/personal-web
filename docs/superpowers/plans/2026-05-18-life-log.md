# Life Log Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the first isolated `life-log/` workspace with Markdown entries, parsing, generated snapshot/preview, and tests.

**Architecture:** `life-log/` owns source Markdown entries and generation code. Tests exercise parser behavior from root `node --test`. The main `frontend/src` remains untouched in this slice.

**Tech Stack:** Node.js ESM, built-in `node:test`, Markdown with frontmatter, static JSON/HTML generation.

---

## File Structure

- `life-log/data/entries/2026-05-17-wedding.md`
  - first real sample entry from the wedding reflection
- `life-log/src/parse-entry.mjs`
  - pure parser for frontmatter and required Markdown sections
- `life-log/scripts/build-snapshot.mjs`
  - reads entries, writes `life-log/generated/entries.json`, writes a standalone `life-log/generated/index.html` preview
- `life-log/generated/entries.json`
  - generated snapshot for preview and future frontend integration
- `life-log/generated/index.html`
  - minimal standalone preview for validating display shape
- `tests/lifeLogParser.test.mjs`
  - parser contract tests
- `package.json`
  - adds `build:life-log`
- `docs/features/life-log.md`
  - update file structure/change notes after implementation

---

### Task 1: Parser Tests

**Files:**
- Create: `tests/lifeLogParser.test.mjs`

- [ ] **Step 1: Write failing parser tests**

```js
import test from 'node:test';
import assert from 'node:assert/strict';
import { parseLifeLogEntry } from '../life-log/src/parse-entry.mjs';

const validEntry = `---
date: 2026-05-17
title: 婚礼、正反馈和一块拼图
mood: 感慨
source: voice-transcript
visibility: private
tags:
  - 婚礼
  - 探索生活
---

## 今日发生的事

- 去参加婚礼。
- 想到未来的拼图签到。

## 今日核心感悟

我还在学习如何从别人的正反馈里走向自己的确认。

## 整理版

今天参加婚礼，看到朋友进入人生的新阶段。

## 札记版

那块拼图像是人生里每段关系留下的位置。

## 原始记录

今天去参加贝贝和他老婆的婚礼了。
`;

test('parseLifeLogEntry extracts frontmatter, sections, bullets, and excerpt', () => {
  const entry = parseLifeLogEntry(validEntry, '2026-05-17-wedding.md');

  assert.equal(entry.slug, '2026-05-17-wedding');
  assert.equal(entry.date, '2026-05-17');
  assert.equal(entry.title, '婚礼、正反馈和一块拼图');
  assert.equal(entry.mood, '感慨');
  assert.deepEqual(entry.tags, ['婚礼', '探索生活']);
  assert.deepEqual(entry.sections.events, ['去参加婚礼。', '想到未来的拼图签到。']);
  assert.equal(entry.sections.insight, '我还在学习如何从别人的正反馈里走向自己的确认。');
  assert.equal(entry.excerpt, '我还在学习如何从别人的正反馈里走向自己的确认。');
});

test('parseLifeLogEntry rejects entries missing a required section', () => {
  const missingRaw = validEntry.replace(/\\n## 原始记录\\n\\n今天去参加贝贝和他老婆的婚礼了。\\n/, '\\n');

  assert.throws(
    () => parseLifeLogEntry(missingRaw, 'bad.md'),
    /Missing required section: 原始记录/
  );
});

test('parseLifeLogEntry rejects entries missing required frontmatter', () => {
  const missingTitle = validEntry.replace('title: 婚礼、正反馈和一块拼图\\n', '');

  assert.throws(
    () => parseLifeLogEntry(missingTitle, 'bad.md'),
    /Missing required frontmatter: title/
  );
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `node --test tests/lifeLogParser.test.mjs`

Expected: FAIL with module not found for `life-log/src/parse-entry.mjs`.

---

### Task 2: Parser Implementation

**Files:**
- Create: `life-log/src/parse-entry.mjs`

- [ ] **Step 1: Implement parser**

Implement:

- `parseLifeLogEntry(markdown, filename)`
- frontmatter scalar values
- frontmatter list values
- required frontmatter validation
- required section validation
- H2 section splitting
- event bullet extraction
- slug from filename
- excerpt from `今日核心感悟`

- [ ] **Step 2: Run parser test**

Run: `node --test tests/lifeLogParser.test.mjs`

Expected: PASS.

---

### Task 3: Sample Entry And Build Script

**Files:**
- Create: `life-log/data/entries/2026-05-17-wedding.md`
- Create: `life-log/scripts/build-snapshot.mjs`
- Generate: `life-log/generated/entries.json`
- Generate: `life-log/generated/index.html`
- Modify: `package.json`

- [ ] **Step 1: Add the sample Markdown entry**

Use the 2026-05-17 wedding reflection with required frontmatter and sections.

- [ ] **Step 2: Add build script**

The script should:

- read `life-log/data/entries/*.md`
- parse with `parseLifeLogEntry`
- sort entries by `date` descending, then `slug`
- write formatted JSON to `life-log/generated/entries.json`
- write a minimal readable `life-log/generated/index.html`

- [ ] **Step 3: Add package script**

Add:

```json
"build:life-log": "node life-log/scripts/build-snapshot.mjs"
```

- [ ] **Step 4: Generate snapshot**

Run: `npm run build:life-log`

Expected: output says generated one entry and writes both generated files.

---

### Task 4: Verification And Docs Update

**Files:**
- Modify: `docs/features/life-log.md`

- [ ] **Step 1: Run focused tests**

Run: `node --test tests/lifeLogParser.test.mjs`

Expected: PASS.

- [ ] **Step 2: Run root tests**

Run: `npm test`

Expected: PASS.

- [ ] **Step 3: Update feature doc**

Update `docs/features/life-log.md` so `File Structure`, `Current Design`, and `Change Notes` match shipped first-slice files.

- [ ] **Step 4: Commit**

Run:

```bash
git add life-log package.json tests/lifeLogParser.test.mjs docs/features/life-log.md docs/superpowers/plans/2026-05-18-life-log.md
git commit -m "feat: add isolated life log workspace"
```

Expected: commit succeeds.
